import { CanActivate, CanActivateChild, CanLoad } from '@angular/router';
import { SplitService } from './splitio.service';
export declare class SplitioGuard implements CanActivate, CanLoad, CanActivateChild {
    private splitService;
    constructor(splitService: SplitService);
    canActivate(): boolean;
    canLoad(): boolean;
    canActivateChild(): boolean;
}
